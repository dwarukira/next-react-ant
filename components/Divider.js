const Divider = ({ paddingBottom = 16, paddingTop = 16 }) => (
  <div
    style={{ paddingBottom: paddingTop, marginBottom: paddingBottom }}
    className="ant-border-bottom"
  />
);

export default Divider;
